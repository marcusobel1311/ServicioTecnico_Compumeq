# ─── Supabase ────────────────────────────────────────────────────────────────
# URL del proyecto Supabase y clave anónima pública (segura para el frontend).
VITE_SUPABASE_URL="https://mpqsaacbusjieeudktev.supabase.co"
VITE_SUPABASE_ANON_KEY="tu_anon_key_de_supabase"

# ─── Email (Vercel Serverless Function / servidor local) ─────────────────────
# Credenciales de Gmail para el envío de correos.
# ⚠️  NUNCA subir valores reales aquí. Configurar en el panel de Vercel.
EMAIL_USER="tucorreo@gmail.com"
EMAIL_PASS="tu_app_password_de_gmail"

# ─── URL de la API de correo ──────────────────────────────────────────────────
# Dejar VACÍO en producción (Vercel) — usa ruta relativa /api/enviar automáticamente.
# En desarrollo local (sin Vite proxy): http://localhost:3001
VITE_EMAIL_API_URL=
